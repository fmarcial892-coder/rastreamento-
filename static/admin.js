document.querySelectorAll('.order-card').forEach(card=>{card.querySelectorAll('[data-status]').forEach(button=>button.addEventListener('click',async()=>{button.disabled=true;const r=await fetch(`/api/admin/orders/${card.dataset.orderId}/status`,{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({status:button.dataset.status})});if(r.ok)location.reload();else{button.disabled=false;alert('Não foi possível atualizar o status.')}}))});
